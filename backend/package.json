{
  "name": "y",
  "version": "1.0.0",
  "description": "* Node.js (https://nodejs.org/en/download/) * npm * nodemon (optional)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
